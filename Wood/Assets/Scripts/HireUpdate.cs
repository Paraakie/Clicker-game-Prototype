using UnityEngine;
using UnityEngine.UI;

public class HireUpdate : MonoBehaviour
{
    /*
     * All Hire Objects
     */
    /* Ranger */
    public Object ranger;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
